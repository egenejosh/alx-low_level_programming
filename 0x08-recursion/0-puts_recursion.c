#include "main.h"

/**
 * _puts_recursion - Prints a string*
 * @s: pointer to the char array which :wq
 * is the string
 * Retur

{

	if (*s != '\0')

		{

			    _putchar(*s);

				    _puts_recursion(s + 1);

					}

	    else

		    {

				_putchar('\n');

				    }

}
